<p style="color: green"><%= notice %></p>

<%= render @task %>

<div>
    <%= link_to t("tasks.show.edit_task"), edit_task_path(@task) %> |
    <%= link_to t("tasks.show.back_to_tasks"), tasks_path %>

    <%= button_to t("tasks.show.delete_task"), @task, method: :delete %>
</div>